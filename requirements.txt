# =============================================================
# AI Business Intelligence Agent — Python Dependencies
# =============================================================

# ----- Core Backend -----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
python-multipart>=0.0.12       # file uploads

# ----- Data Processing -----
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=17.0.0                # parquet support

# ----- ML Engine (Milestone 2) -----
prophet>=1.1.6
mlflow>=2.18.0
scikit-learn>=1.5.0           # Isolation Forest (Engineer 2 — anomaly detection)
joblib>=1.4.0                 # model artifact (de)serialisation
xgboost>=2.1.0                # optional alternative anomaly model

# ----- RAG & Generative AI (Milestone 3) -----
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0          # Groq provider
langchain-openai>=0.2.0        # Azure OpenAI provider
chromadb>=0.6.0
sentence-transformers>=3.3.0   # local embeddings, no API key required

# ----- Data Validation -----
great-expectations>=1.3.0

# ----- Utilities -----
httpx>=0.28.0
